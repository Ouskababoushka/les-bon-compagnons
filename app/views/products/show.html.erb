<p><%= @product.name %> // <%= number_to_currency(@product.price) %>Eur</p>
<p>
  <%= link_to "Modifier", edit_product_path(@product) %> |
  <%= link_to "Supprimer", product_path(@product), method: :delete, data: {confirm: "Etes vous sûr compagnon ?"} %>
  <%= button_to "Ajouter #{@product.name} au panier", line_items_path(product_id: @product.id) %>
</p>

<%= link_to "Ajouter un Produit", new_product_path %>
